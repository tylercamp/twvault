apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "twvault.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "twvault.name" . }}
    helm.sh/chart: {{ include "twvault.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "twvault.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
